# インストール時のスクリプト（preinstall / postinstall等）の自動実行を禁止
ignore-scripts=true

# パッケージの整合性・チェックサム（lockファイル）を厳格に検証
engine-strict=true

# セキュリティアラートの警告表示を有効化
audit=true

# package.json への依存追加時に ^ / ~ を付けずバージョンを固定する
save-exact=true