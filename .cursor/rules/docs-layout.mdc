---
description: ドキュメント配置。AI 用は .cursor、設計書は docs。
alwaysApply: true
---

# ドキュメント配置

- AI が利用するルール・指針は `.cursor/` 配下に置く。
- 設計書など仕様ドキュメントは `docs/` 配下で管理する。
- `.docs` は使わない。作成・参照・更新もしない（`docs` との二重管理になるため）。
